## Find Largest Value

This program reads a sequence of numeric values from user input, stores them in a list, and identifies the largest value.

### Features
- Accepts values until the user enters `Q`
- Determines the largest number in the list
- Highlights the largest value in the output
