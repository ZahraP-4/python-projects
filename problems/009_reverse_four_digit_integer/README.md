# Reverse Four-Digit Integer

Reads a four-digit integer from standard input and prints its digits in reverse order, one per line.

**Input:**  
- A four-digit integer

**Output:**  
- Reversed digits, each on a separate line
