## Dictionary Keys, Values, and Average

This program iterates over a dictionary to display all keys, values, key–value pairs (including sorted order), and calculates the average of the values.

### Features
- Prints dictionary keys and values
- Displays key–value pairs in original and sorted order
- Computes the average of numeric values
